{
  "name": "omarchy-capture-conveyor-entry",
  "version": "0.1.0",
  "private": true,
  "description": "Local Omarchy screenshot inbox with a node test harness",
  "scripts": {
    "test": "perl -c bin/capture-conveyor-scan && c8 --all --include=Model.js --check-coverage --lines 95 --functions 95 --branches 90 node --test tests/*.test.js"
  },
  "devDependencies": {
    "c8": "^10.1.3"
  }
}
